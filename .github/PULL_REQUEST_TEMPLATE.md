## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] `uv run pytest` passes
- [ ] Verified locally (`docker compose up --build -d`) before pushing
- [ ] README / `docs/` updated if behavior or setup changed
- [ ] PR is labeled `enhancement`, `bug`, or `documentation` (drives release notes)
